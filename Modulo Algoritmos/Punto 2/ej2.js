let contrasena = prompt("Crea una contraseña");

 while (i );{
let contrasena1 = window.prompt("Repite la contraseña");

if (contrasena === contrasena1 ) {
    
}

 }

let contrasena1 = window.prompt("Repite la contraseña");
let contrasena2 = window.prompt("Repite la contraseña");
let contrasena3 = window.prompt("Repite la contraseña");
let respuesta = true;

if(contrasena1 === contrasena2 && contrasena2 === contrasena3){
    alert("Felicitaciones, recordás tu contraseña")
}else {
    alert("Tenes que ejercitar la memoria")
}